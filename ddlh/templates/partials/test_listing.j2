<div class="container">
    <h1>
        Distributed Design Learning Hub
        {% if subtitle is defined %}— <em>{{ subtitle }}</em>{% endif %}
    </h1>
    <p>
        <strong>Themes:</strong>
        {% for theme in themes %}<a href="?theme={{ theme|urlencode }}">{{ theme }}</a>,{% endfor %}
    </p>
    <p>
        <strong>Tags:</strong>
        {% for tag in tags %}<a href="?tag={{ tag|urlencode }}">{{ tag }}</a>,{% endfor %}
    </p>
    <hr />
    <ul>
        {% for document in documents %}
            <li>{% include "partials/document.j2" %}</li>
        {% endfor %}
    </ul>
</div>
